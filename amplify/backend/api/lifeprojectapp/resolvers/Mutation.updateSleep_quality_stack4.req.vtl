#set( $updateList = {} )
#foreach( $entry in $ctx.args.updateSleep_quality_stack4Input.keySet() )
  #set( $discard = $updateList.put($entry, "'$ctx.args.updateSleep_quality_stack4Input[$entry]'") )
#end
#set( $update = $updateList.toString().replace("{","").replace("}","") )
{
  "version": "2018-05-29",
  "statements":   ["UPDATE Sleep_quality_stack4 SET $update WHERE id=$ctx.args.updateSleep_quality_stack4Input.id", "SELECT * FROM Sleep_quality_stack4 WHERE id='$ctx.args.updateSleep_quality_stack4Input.id'"]
}