#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createReligiousnessInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createReligiousnessInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Religiousness $colStr VALUES $valStr", "SELECT * FROM Religiousness WHERE userid='$ctx.args.createReligiousnessInput.userid'"]
}