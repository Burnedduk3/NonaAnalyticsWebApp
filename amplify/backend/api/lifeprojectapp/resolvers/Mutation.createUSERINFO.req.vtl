#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createUSERINFOInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createUSERINFOInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO USERINFO $colStr VALUES $valStr", "SELECT * FROM USERINFO WHERE id=$ctx.args.createUSERINFOInput.id"]
}