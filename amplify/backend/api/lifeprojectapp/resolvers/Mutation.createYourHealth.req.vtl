#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createYourHealthInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createYourHealthInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO YourHealth $colStr VALUES $valStr", "SELECT * FROM YourHealth WHERE userid='$ctx.args.createYourHealthInput.userid'"]
}