#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createEmploymentInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createEmploymentInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Employment $colStr VALUES $valStr", "SELECT * FROM Employment WHERE userid='$ctx.args.createEmploymentInput.userid'"]
}