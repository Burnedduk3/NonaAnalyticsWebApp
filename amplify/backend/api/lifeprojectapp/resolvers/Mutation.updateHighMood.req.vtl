#set( $updateList = {} )
#foreach( $entry in $ctx.args.updateHighMoodInput.keySet() )
  #set( $discard = $updateList.put($entry, "'$ctx.args.updateHighMoodInput[$entry]'") )
#end
#set( $update = $updateList.toString().replace("{","").replace("}","") )
{
  "version": "2018-05-29",
  "statements":   ["UPDATE HighMood SET $update WHERE userid=$ctx.args.updateHighMoodInput.userid", "SELECT * FROM HighMood WHERE userid='$ctx.args.updateHighMoodInput.userid'"]
}