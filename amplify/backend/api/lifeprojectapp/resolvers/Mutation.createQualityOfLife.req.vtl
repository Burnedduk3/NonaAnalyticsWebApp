#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createQualityOfLifeInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createQualityOfLifeInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO QualityOfLife $colStr VALUES $valStr", "SELECT * FROM QualityOfLife WHERE userid='$ctx.args.createQualityOfLifeInput.userid'"]
}