#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createOralHealthInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createOralHealthInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO OralHealth $colStr VALUES $valStr", "SELECT * FROM OralHealth WHERE userid='$ctx.args.createOralHealthInput.userid'"]
}