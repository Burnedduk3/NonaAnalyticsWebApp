#set( $updateList = {} )
#foreach( $entry in $ctx.args.updateSocial_network_stack2Input.keySet() )
  #set( $discard = $updateList.put($entry, "'$ctx.args.updateSocial_network_stack2Input[$entry]'") )
#end
#set( $update = $updateList.toString().replace("{","").replace("}","") )
{
  "version": "2018-05-29",
  "statements":   ["UPDATE Social_network_stack2 SET $update WHERE id=$ctx.args.updateSocial_network_stack2Input.id", "SELECT * FROM Social_network_stack2 WHERE id='$ctx.args.updateSocial_network_stack2Input.id'"]
}