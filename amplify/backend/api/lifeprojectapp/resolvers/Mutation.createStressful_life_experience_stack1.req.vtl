#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createStressful_life_experience_stack1Input.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createStressful_life_experience_stack1Input[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Stressful_life_experience_stack1 $colStr VALUES $valStr", "SELECT * FROM Stressful_life_experience_stack1 WHERE id='$ctx.args.createStressful_life_experience_stack1Input.id'"]
}