#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createReligiousness_spirituality_stack8Input.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createReligiousness_spirituality_stack8Input[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Religiousness_spirituality_stack8 $colStr VALUES $valStr", "SELECT * FROM Religiousness_spirituality_stack8 WHERE id='$ctx.args.createReligiousness_spirituality_stack8Input.id'"]
}