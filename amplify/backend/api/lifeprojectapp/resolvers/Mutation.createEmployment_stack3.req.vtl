#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createEmployment_stack3Input.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createEmployment_stack3Input[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Employment_stack3 $colStr VALUES $valStr", "SELECT * FROM Employment_stack3 WHERE id='$ctx.args.createEmployment_stack3Input.id'"]
}