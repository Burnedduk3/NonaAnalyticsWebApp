#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createPetsInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createPetsInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Pets $colStr VALUES $valStr", "SELECT * FROM Pets WHERE userid='$ctx.args.createPetsInput.userid'"]
}