#set( $updateList = {} )
#foreach( $entry in $ctx.args.updateEmployment_stack3Input.keySet() )
  #set( $discard = $updateList.put($entry, "'$ctx.args.updateEmployment_stack3Input[$entry]'") )
#end
#set( $update = $updateList.toString().replace("{","").replace("}","") )
{
  "version": "2018-05-29",
  "statements":   ["UPDATE Employment_stack3 SET $update WHERE id=$ctx.args.updateEmployment_stack3Input.id", "SELECT * FROM Employment_stack3 WHERE id='$ctx.args.updateEmployment_stack3Input.id'"]
}