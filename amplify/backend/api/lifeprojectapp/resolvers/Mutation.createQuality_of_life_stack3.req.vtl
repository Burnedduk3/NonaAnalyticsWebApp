#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createQuality_of_life_stack3Input.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createQuality_of_life_stack3Input[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Quality_of_life_stack3 $colStr VALUES $valStr", "SELECT * FROM Quality_of_life_stack3 WHERE id='$ctx.args.createQuality_of_life_stack3Input.id'"]
}