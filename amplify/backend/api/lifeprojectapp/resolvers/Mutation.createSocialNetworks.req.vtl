#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createSocialNetworksInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createSocialNetworksInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO SocialNetworks $colStr VALUES $valStr", "SELECT * FROM SocialNetworks WHERE userid='$ctx.args.createSocialNetworksInput.userid'"]
}