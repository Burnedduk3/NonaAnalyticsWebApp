import React from 'react';
import './styles.scss';

const PublicHeader = () => (
  <header className="header">
    <div className="header-container" />
    <h1 className="main-title">We want to build a community that promotes well-being</h1>
    <button type="button" className="call-to-action">Learn more</button>
  </header>
);

export default PublicHeader;
